# 第1周开发总结（2025-11-15）

## 基本信息
- 开发周期: 第1周（初始化与环境搭建）
- 总结日期: 2025-11-15

## 本周开发目标
- 初始化前后端项目骨架，完成本地数据库创建与连接配置，跑通联调健康检查。

## 已完成内容
- 后端（Spring Boot 3.3.5，JDK 17）
  - 新建 Maven 项目 `food-sharing-backend`
  - 配置依赖：Web、JPA、Security、Validation、MySQL、Lombok
  - 配置 `application.yml`（端口 8081，MySQL 连接：root/123456）
  - 创建 `FoodSharingApplication` 启动类
  - 创建 `HealthController`（GET /api/v1/health）
  - 安全与跨域：`SecurityConfig` 允许全部请求；`WebConfig` 允许 http://localhost:5173 跨域
  - 启动服务：运行在 8081（8080 被占用，已切换）

- 前端（Vue 3 + Vite + Element Plus）
  - 新建 Vite 项目骨架 `food-sharing-frontend`
  - 配置依赖：vue、vue-router、pinia、axios、element-plus、@vitejs/plugin-vue、vite
  - 目录与文件：`index.html`、`src/main.js`、`src/App.vue`、`src/router/index.js`、`src/store/index.js`、`src/api/request.js`
  - 页面：`Home.vue`（按钮调用后端 /api/v1/health）、`Login.vue`、`Register.vue`
  - axios baseURL 指向 `http://localhost:8081/api/v1`
  - 启动开发服务器：运行在 http://localhost:5173

- 数据库
  - 创建数据库：`food_sharing`（utf8mb4）
  - JPA ddl-auto: update（尚未创建实体，暂不建表）

## 进行中/遗留项
- Redis 未安装与接入（本机未检测到 redis-cli，后续可补）
- 基础实体（如 User）与仓库尚未创建（计划在第2周完成）

## 测试情况
- 前端启动成功（Vite 本地 5173）
- 后端启动成功（Spring Boot 本地 8081）
- 健康检查：前端首页点击“后端健康检查”可返回 status: ok

## 下周计划（第2周）
- 账号体系：注册/登录、JWT、密码加密、权限控制、路由守卫
- 接口：/auth/register、/auth/login、/users/me
- 前端：登录/注册页联调、状态保存、鉴权跳转

## 备注
- 如遇端口冲突，可修改前端端口（vite.config.js）或后端端口（application.yml）。
